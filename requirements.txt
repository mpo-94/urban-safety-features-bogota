# Direct dependencies only, pinned to the versions the analysis was run with.
# Transitive dependencies are resolved by pip and deliberately left unpinned,
# so this file stays readable and reviewable. Requires Python 3.12: part of the
# geospatial stack has no wheels for 3.13+ yet.

# Data processing
pandas==3.0.5
numpy==2.5.1
pyarrow==25.0.0

# Geospatial. pyogrio is the I/O engine geopandas uses to read shapefiles,
# rtree the spatial index backend that makes sjoin viable on crash points.
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0
rtree==1.4.1
mapclassify==2.10.0

# Visualization
matplotlib==3.11.1
seaborn==0.13.2

# Modeling. statsmodels covers the GLMs, linearmodels the panel estimators.
statsmodels==0.14.6
scipy==1.18.0
linearmodels==7.0

# Notebooks. Notebooks only call into src/, but the kernel and widgets are
# needed to run them at all.
jupyterlab==4.6.2
ipykernel==7.3.0
ipywidgets==8.1.8
